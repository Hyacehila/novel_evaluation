# 快速开始

本文档面向第一次运行项目的用户，目标是在几分钟内完成本地启动和一次评测提交。

## 运行前准备

当前官方验证环境为 `Windows + PowerShell`。请先安装：

- `Python 3.13`
- [`uv`](https://docs.astral.sh/uv/)
- `Node.js 20+`
- `pnpm`

## 1. 安装依赖

在仓库根目录执行：

```powershell
.\scripts\setup.ps1
```

该脚本会：

- 安装 `apps/api` 依赖
- 安装 `apps/worker` 依赖
- 安装 `apps/web` 依赖
- 若仓库根目录没有 `.env`，自动从 `.env.example` 复制一份

## 2. 启动 API

新开一个 PowerShell 窗口，在仓库根目录执行：

```powershell
.\scripts\run-api.ps1
```

默认地址：`http://127.0.0.1:8000`

## 3. 启动 Web

再开一个 PowerShell 窗口，在仓库根目录执行：

```powershell
.\scripts\run-web.ps1
```

默认地址：`http://127.0.0.1:3000`

## 4. 提交第一份评测

打开 `http://127.0.0.1:3000/tasks/new`，填入一份标题、正文和大纲。第一次体验不需要 API Key。

示例正文：

```text
第一章里，青岚宗护山大阵突然熄灭，外门弟子顾辞在山门值守时亲眼看见长老重伤归来。执法堂宣布七天后宗门大比照常举行，若无人夺得秘境名额，宗门将被邻宗吞并。顾辞为了保住师父留下的药园，只能冒险借用残缺功法突破境界，同时调查阵法失效的真相。短短一夜里，他先后失去住处、同伴和退路，被迫主动卷入更大的权力斗争。
```

示例大纲：

```text
后续主线围绕宗门大比、秘境夺宝和宗门重建展开。顾辞会先在大比中证明自己，再进入秘境寻找修复大阵的核心灵物，同时逐步揭开长老叛变与外敌联手的内幕。中期通过师徒关系、同门竞争和资源争夺持续升级冲突，后期把宗门存亡、个人成长和秘宝兑现收束到同一条升级主线上。
```

## 5. 你应该看到什么

- 新建任务后会跳到任务详情页
- 任务状态会从 `queued / processing` 进入 `completed`
- 结果可用时，结果页会展示签约概率、平台建议、编辑结论和详细分析

默认情况下，这次结果来自 deterministic fallback，用于快速验证本地流程和持久化链路，不用于真实生产评审。

## 自定义配置

如果你想改端口、数据库路径或日志目录：

```powershell
Copy-Item .env.example .env
```

然后修改 `.env` 中对应的变量，再重新启动脚本。

## 下一步

- 想接入真实模型：看 `real-provider.md`
- 想排查常见问题：看 `faq.md`
- 想做维护者 smoke 或发布前检查：看 `../operations/local-installation-and-smoke.md`
