# `evals`

该目录用于承载正式评测与回归体系。

## 目标

- 让模型切换、Prompt 调整、Schema 演进都可以做稳定性验证
- 保留样本、基线、执行入口和报告产物
- 支持结构合法性、评分稳定性、鲁棒性与回归验证

## 子目录说明

- `datasets/`：样本、金标准、测试夹具
- `cases/`：结构化评测用例
- `runners/`：评测执行入口
- `reports/`：评测报告输出
- `baselines/`：历史基线结果
