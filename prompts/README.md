# `prompts`

本目录是正式 Prompt 资产真源，只保留当前评分主线：

- `registry/`
- `versions/`
- `scoring/`

约束：

- 正文用 `Markdown`
- 元数据用 `YAML`
- `rubric` 和 `aggregation` 只允许输出当前 schema 需要的字段
- 不维护历史字段别名或未被当前 schema 接受的输出形状

继续阅读：

- [`../docs/prompts-and-evals.md`](../docs/prompts-and-evals.md)
